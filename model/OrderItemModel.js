export class OrderItemModel {
    constructor(itemId, itemName, qtv, price) {
        this.itemId = itemId;
        this.itemName = itemName;
        this.qtv = qtv;
        this.price = price;
    }
}